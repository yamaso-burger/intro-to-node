// The Node RemotePlayback(Read Evaluation Print Loops)

// Flie system module. It enables us to read and write on local files
const fs = require("fs");

fs.copyFileSync("file.txt", "file2.txt");